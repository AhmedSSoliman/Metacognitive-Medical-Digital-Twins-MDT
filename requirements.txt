# Core Dependencies
# Pin to versions with available Python 3.13 wheels; install with `--index-url https://download.pytorch.org/whl/cu118` for CUDA wheels.
torch==2.9.1
torchvision==0.24.1
# Align torchaudio to the same torch release to avoid resolver conflicts
torchaudio==2.9.1
transformers>=4.35.0
peft>=0.7.0
datasets>=2.14.0
accelerate>=0.25.0

# Reward Components
sentence-transformers>=2.2.0
rouge-score>=0.1.2
bert-score>=0.3.13
textstat>=0.7.3

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Web Interface
gradio>=4.0.0
fastapi>=0.110.0
uvicorn>=0.27.0
httpx>=0.27.0
streamlit>=1.60.0  # scripts/streamlit_app.py -- multi-tab phase tester (chat, hypergraph, tool use, reward inspector)

# Utilities
tqdm>=4.65.0
python-docx>=1.1.0

# Testing
pytest>=8.0.0