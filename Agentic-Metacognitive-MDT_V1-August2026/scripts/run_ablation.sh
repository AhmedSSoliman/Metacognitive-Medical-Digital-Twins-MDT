#!/usr/bin/env bash
# Ablation study launcher -- PLACEHOLDER.
#
# Status: no ablation runner (Python or shell) exists in the source
# repository (Agentic-DT_V1-July) to port. See
# evaluation/ablation.py for the full explanation and grep evidence.
#
# This script is a stub so the intended entry point exists ahead of the
# actual ablation-runner implementation. It intentionally fails fast rather
# than silently doing nothing.

set -euo pipefail

echo "ERROR: scripts/run_ablation.sh is a placeholder." >&2
echo "No ablation-study runner exists in the source repository" >&2
echo "(Agentic-DT_V1-July) to port. See evaluation/ablation.py for details." >&2
exit 1
